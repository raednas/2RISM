<form method="post" action="{{ path('app_typepack_delete', {'id': typepack.idTypepack}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typepack.idTypepack) }}">
    <input type="hidden" name="_method" value="DELETE"> <!-- Ajoutez cette ligne pour indiquer la méthode DELETE -->
    <button type="submit" class="btn btn-danger">Delete</button> <!-- Modifiez la classe pour utiliser un style de bouton de suppression -->
</form>