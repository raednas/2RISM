<form method="post" action="{{ path('app_guide_delete', {'id_Guide': guide.idGuide}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guide.idGuide) }}">
    <button class="btn">Delete</button>
</form>
