{% extends '/back/base.html.twig' %}

{% block title %}Guide{% endblock %}

{% block body %}
    <h1>Guide</h1>

    <div class="table-responsive">
        <table class="table table-striped">
            <tbody>
                <tr>
                    <th>Id_Guide</th>
                    <td>{{ guide.idGuide }}</td>
                </tr>
                <tr>
                    <th>Nom_Guide</th>
                    <td>{{ guide.NomGuide }}</td>
                </tr>
                <tr>
                    <th>Prenom_Guide</th>
                    <td>{{ guide.PrenomGuide }}</td>
                </tr>
                <tr>
                    <th>Age_Guide</th>
                    <td>{{ guide.AgeGuide }}</td>
                </tr>
                <tr>
                    <th>Sexe_Guide</th>
                    <td>{{ guide.SexeGuide }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <a href="{{ path('app_guide_index') }}" class="btn btn-warning" style="border-radius: 10px;">back to list</a>

    <a href="{{ path('app_guide_edit', {'id_Guide': guide.idGuide}) }}" class="btn btn-primary" style="border-radius: 10px;">edit</a>

    {{ include('guide/_delete_form.html.twig') }}
{% endblock %}