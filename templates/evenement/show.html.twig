{% extends 'back.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_event</th>
                <td>{{ evenement.idEvent }}</td>
            </tr>
            <tr>
                <th>Type_event</th>
                <td>{{ evenement.typeEvent }}</td>
            </tr>
            <tr>
                <th>Duree_event</th>
                <td>{{ evenement.dureeEvent }}</td>
            </tr>
            <tr>
                <th>Date_event</th>
                <td>{{ evenement.dateEvent ? evenement.dateEvent|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Nom_event</th>
                <td>{{ evenement.nomEvent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evenement_index') }}"class="btn btn-warning" style="border-radius: 10px;">back to list</a>

    <a href="{{ path('app_evenement_edit', {'id_event': evenement.idEvent}) }}"class="btn btn-primary" style="border-radius: 10px;">edit</a>

    {{ include('evenement/_delete_form.html.twig') }}
{% endblock %}
