<form method="post" action="{{ path('app_hebergement_controller2_delete', {'Id': hebergement.Id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hebergement.Id) }}">
    <button class="btn">Delete</button>
</form>
