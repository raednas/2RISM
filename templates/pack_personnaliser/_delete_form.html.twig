<form method="post" action="{{ path('app_pack_personnaliser_delete', {'idPackPersonnaliser': pack_personnaliser.idPackPersonnaliser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pack_personnaliser.idPackPersonnaliser) }}">
    <button class="btn">Delete</button>
</form>
